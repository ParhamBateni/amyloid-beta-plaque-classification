{
    "model_name": "pi_model",
    "training": {
        "consistency_lambda_max": 0.001,
        "consistency_loss_type": "mse",
        "ramp_up_epochs": 100,
        "ramp_up_function": "linear"
    },
    "hyperparameter_tuning": {
        "training": {
            "consistency_lambda_max": [0.001, 0.005, 0.01],
            "ramp_up_epochs": [50, 100],
            "ramp_up_function": ["linear", "sigmoid", "fixed"],
            "consistency_loss_type": ["mse", "kl"]
        }
    }
}
