{
    "model_name": "pi_model",
    "training": {
        "consistency_lambda_max": 0.1,
        "consistency_loss_type": "cross_entropy",
        "ramp_up_epochs": 100,
        "ramp_up_function": "linear"
    },
    "hyperparameter_tuning": {
        "model_name": ["mean_teacher"],
        "training": {
            "consistency_lambda_max": [0.05, 0.1, 0.2, 0.5],
            "ramp_up_epochs": [10, 50, 100],
            "ramp_up_function": ["linear", "sigmoid", "fixed"],
            "consistency_loss_type": ["mse", "kl", "cross_entropy"]
        }
    }
}
